
const defaultState = {
    items: [],
    isFetchiung: true
}

export default function Reducer (state = defaultState, action){
    switch(action.type){

        default:
            return state
    }
}